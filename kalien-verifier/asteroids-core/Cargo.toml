[package]
name = "asteroids-verifier-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", default-features = false, features = ["derive", "alloc"] }
stellar-strkey = { version = "0.0.16", default-features = false }

[features]
default = ["std"]
std = []
